---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span class:list={['wordmark', className]}>
  MORA<span class="wordmark-dot" aria-hidden="true"></span>
</span>
